name: Test

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

permissions: {}

jobs:
  test:
    permissions:
      contents: read
    uses: luxass/shared-workflows/.github/workflows/reusable-test.yaml@7be4a924607be148a2093d4651d920cfcc9a264f # v0.13.0
