# Anti-slop provenance

- Installed from the bundled anti-slop asset in
  `/Users/lucasnorgard/dots/home/.agents/skills/install-anti-slop/assets/anti-slop`.
- Installed at `tools/oxlint/anti-slop/`.
- Generic rules and the Effect-specific rules are enabled through
  `oxlint.config.ts`.
- `@oxlint/plugins` is pinned to `1.83.0`, matching the repository's Oxlint
  version.
- The bundled source revision is not exposed by the installer; the pristine
  copied tree is the recoverable source snapshot for this installation.
